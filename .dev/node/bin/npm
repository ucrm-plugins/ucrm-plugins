#!/usr/bin/env bash
INCLUDE_DIR=$(realpath "$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"/../..)

# shellcheck disable=SC1090
source "$INCLUDE_DIR"/include.sh node

# shellcheck disable=SC2086
docker run $DOCKER_ARGS $DOCKER_NAME $PROXY_BIN npm "$@"
