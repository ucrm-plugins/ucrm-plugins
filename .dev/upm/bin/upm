#!/usr/bin/env bash
PROJECT_DIR=$(realpath "$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"/../../..)
BIN_TARGET="$PROJECT_DIR/.dev/upm/upm.php"
COMPOSER_RUNTIME_BIN_DIR="$PROJECT_DIR/vendor/bin"

php "$BIN_TARGET" --ansi "$@"
