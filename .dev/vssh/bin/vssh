#!/usr/bin/env bash
PROJECT_DIR=$(realpath "$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"/../../..)

SSH_PATH=~/.ssh/config
SSH_HOST=uisp-dev

ssh -F "$SSH_PATH" "$SSH_HOST" "$@"
